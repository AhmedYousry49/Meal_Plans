specVersion: "v1"
serviceId: "mealplans"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mealplans-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
